<!--pages/projectdetail/projectdetail.wxml-->
<import src="./../../wxParse/wxParse.wxml" />
<view class="project-detail">
    <view class="project-hd">
        <image class="cover-picture" src="{{imgHost}}{{coverPicture}}" mode="aspectFit"></image>
        <view class="article-title">{{articleTitle}}</view>
    </view>
    <view class="article-content">
        <template is="wxParse" data="{{wxParseData:content.nodes}}" />
    </view>
</view>
<!-- <view class="project-detail">
    <rich-text type="text">{{content}}</rich-text>
</view> -->
